---
description: Всегда авто-деплой после изменений
alwaysApply: true
---

# Auto-deploy

После любых изменений в коде — коммит и пуш в `main` (или PR → merge), чтобы сработал автодеплой через GitHub Actions.

Кратко: **изменил код → закоммить → пуш в main → деплой**.
