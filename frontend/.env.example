# Скопируйте в .env.local и замените ссылку на вашу ngrok для бэкенда (порт 8080)
# cp .env.example .env.local

# WebSocket URL. Для ngrok всегда wss:// — иначе Telegram заблокирует.
VITE_WS_URL=wss://ВАШ_ДОМЕН_NGROK_8080/ws

# Username бота для реферальной ссылки. ОБЯЗАТЕЛЬНО укажи своего бота!
# Если /api/config не загрузится — будет использоваться это значение.
VITE_TELEGRAM_BOT_USERNAME=ТвойБот
